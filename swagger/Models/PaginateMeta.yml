components:
  schemas:
    PaginateMeta:
      type: object
      properties:
        id:
          type: number
        name:
          type: string
        key:
          type: string
        parent_id:
          type: number
          nullable: true
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
