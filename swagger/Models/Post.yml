components:
  schemas:
    Post:
      type: object
      properties:
        id:
          type: number
        title:
          type: string
        content:
          type: string
          nullable: true
        userId:
          type: number
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
