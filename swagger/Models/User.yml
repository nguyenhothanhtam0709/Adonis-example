components:
  schemas:
    User:
      type: object
      properties:
        id:
          type: number
        email:
          type: string
          format: email
        remember_me_token:
          type:
            - null
            - string
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time
